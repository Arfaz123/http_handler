name: http_handler
description: HttpHandler provides utility functions and customization options to streamline API request handling in your Flutter applications.
version: 0.0.1
homepage: https://linktr.ee/arfaz_860
repository: https://github.com/Arfaz123/http_handler.git
topics: [api, api-call, http-handler, http]

screenshots:
  - description: 'HttpHandler Essential Flutter Package.'
    path: preview/http_handler_preview_image.png

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^5.0.1
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
